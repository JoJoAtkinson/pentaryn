# Foundry VTT weekly update — Saturday 2026-08-22

**No update run happened this week.** The scheduler fired at 09:22, outside the 03:30–09:00 window, so the run aborted before touching anything. That timing is the signature of a run launchd deferred while the Mac was asleep, not the scheduled Saturday run.

## What changed

Nothing. No packages applied, no core or system update, no snapshot taken (so no restore command — none is needed, since nothing was migrated).

## Held

Nothing was held for review. The run skipped before it checked upstream, so this is not "everything was up to date" — it's "nothing was looked at." Anything released this week is still unexamined.

## Drift and flags

No fork drift, local module drift, or adjudication data — none was collected.

## What you'd do about it

Either leave it and let next Saturday's run catch up (it will pick up two weeks of releases at once), or wake the Mac and trigger a run manually today if you want the table current before this evening's session.