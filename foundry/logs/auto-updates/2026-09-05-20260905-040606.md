# Foundry update report — 2026-09-05

## What changed

Nothing. No packages were updated this run; core stayed at 14.367 (stable) and dnd5e at 5.3.3.

No system or core migration ran, so no restore is needed. The snapshot exists if you want it anyway: `make foundry-restore SNAP=world-2026-09-05-040611-fvtt14.367.tar.gz`

## Worth knowing

Nothing — no releases were applied, so there are no bug fixes or new features to report.

## Held

- **[mad-endlesswiz2](https://www.patreon.com/themadcartographer)** — 13.0.1 → 14.1.0 (major). Held for two reasons: it's a protected module this run hadn't seen before, so it gets one human look before it's allowed to update unattended; and there are no release notes published for the jump, which on a major version bump is an automatic hold.

  To clear it: skim the Patreon page for what changed between 13.x and 14.x, then approve it for the next run. If nothing there tells you what a major bump touched, it's reasonable to keep holding it — this is a Mad Cartographer map module, so a bad update most likely shows up as broken scenes rather than data loss.

## Drift and flags

- **Foundry is down.** Start it again: `make vtt-up`
- **Fork `JoJoAtkinson/foundry-vtt-mcp`** (foundry-mcp-bridge) is 16 commits behind `adambdooley/foundry-vtt-mcp` on `master`, with zero local commits ahead — a clean fast-forward: `gh repo sync JoJoAtkinson/foundry-vtt-mcp`
- **`pentaryn-seafoot-maps` 1.0.0** is installed in Foundry but has no source in `foundry/module/`. Either it belongs in the repo and needs committing, or it's a leftover that should be uninstalled.