# Foundry Weekly Update — 2026-08-22

**The run did not happen.** It aborted during the pre-update backup, before any package was inspected or touched.

## What changed

Nothing. No core, system, or module updates were applied.

## Bug fixes and new features

None to report — no release notes were fetched, because the run never reached the update stage.

## What was held and why

Everything was held, by failure rather than by decision:

- The pre-update cloud backup (`scripts.foundry.cloud backup --reason auto-update`) **timed out after 900 seconds** and the run aborted.
- No snapshot was taken, so `restore_command` is `null` — there is no rollback point from this run.
- Because the backup gate never passed, no version checks, updates, or smoke tests ran.

**What you'd have to do:** the world is untouched and safe to play on this morning — nothing was migrated. Before the next run can do anything, the backup needs to succeed. Worth checking whether the cloud backup target is reachable and how large the world has grown; a 15-minute timeout on a backup usually means either a stalled upload or a world that has outgrown the window. If you want updates this weekend, run the backup manually first and confirm it completes, then re-run the updater.

## Drift and other flags

No data. Fork drift, local module drift, and upstream adjudication were all `null` — those checks run after the backup gate, so nothing was measured. Treat last week's drift status as still current.

Note on the standing rule: no third-party release notes were present in this record, so nothing untrusted was read or summarised this week.