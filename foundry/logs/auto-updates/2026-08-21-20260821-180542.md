# Foundry VTT Weekly Update — Saturday, 21 August 2026

## What changed

Nothing. No packages were installed, updated, or rolled back this run.

Core is on **14.365** (stable), dnd5e system on **5.3.3** — both unchanged, so no migration ran and no restore is needed. A snapshot was taken anyway: `world-2026-08-21-180545-fvtt14.365.tar.gz`.

## Worth knowing

Nothing to report — no releases were applied, and the one available update has no release notes.

## Held

- **mad-endlesswiz2** — 13.0.1 → 14.1.0 (major) — [release page](https://www.patreon.com/themadcartographer)
  - Held for two reasons: it's a protected package the automation hasn't seen before, so it needs one manual review before it's allowed to update unattended; and the publisher shipped no release notes alongside a major version jump, which trips the missing-notes-plus-major hold rule.
  - **What you'd do:** check the Patreon post yourself for what changed between 13.x and 14.x, then approve it if it looks safe. A major bump on a map/asset module usually means a data-format change, so do it with the snapshot above at hand.

## Flagged

- **Admin relaunch failed.** The `space-journey` world could not be relaunched: an admin password is configured in `Config/admin.txt` and the one the automation supplied did not match. Re-sync it with `make foundry-admin-push`. Until that's fixed, any run that needs to bounce the server will stall here.
- **Local module drift:** `pentaryn-seafoot-maps` 1.0.0 is installed on the server but has no source in `foundry/module/`. It's unmanaged — nothing tracks or updates it, and it won't survive a rebuild from source.
- **Fork drift:** none. `JoJoAtkinson/foundry-vtt-mcp` is exactly level with `adambdooley/foundry-vtt-mcp` on `master` (0 ahead, 0 behind).