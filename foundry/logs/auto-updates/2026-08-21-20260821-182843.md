# Foundry VTT Weekly Update — 2026-08-21

## What changed

Nothing. No packages were updated this week. Core stayed on **14.367** (stable), dnd5e stayed on **5.3.3**.

A snapshot was still taken before the run: `world-2026-08-21-182846-fvtt14.367.tar.gz`. Restore with `make foundry-restore SNAP=world-2026-08-21-182846-fvtt14.367.tar.gz` if you need it.

## Worth knowing

Nothing to report — no releases were applied, so there are no bug fixes or new features to try.

## Held

- **[mad-endlesswiz2](https://www.patreon.com/themadcartographer)** — 13.0.1 → 14.1.0 (major). Held for two reasons: this is the first run that has seen it (new packages get one review before they're allowed to update unattended), and the publisher shipped **no release notes at all** for a major version bump. A 13 → 14 jump with zero documentation is exactly the case where something silently breaks mid-session.
  **What you'd do:** check the [Patreon page](https://www.patreon.com/themadcartographer) for a changelog or post describing 14.1.0. If it looks safe, tell me to clear the hold and it'll go in next run. It's marked protected, so it will not update on its own until you say so.

## Drift and flags

- **Fork `JoJoAtkinson/foundry-vtt-mcp`** (foundry-mcp-bridge): clean — 0 ahead, 0 behind `adambdooley/foundry-vtt-mcp` on `master`. Nothing to do.
- **Local module drift: `pentaryn-seafoot-maps` v1.0.0** — installed in Foundry but with no corresponding source in `foundry/module/`. It isn't tracked by anything, so it won't be backed up or rebuilt if the install is lost. Worth deciding whether to commit the source or drop the module.

No failures, no recoveries, no smoke-test issues. Nothing in the record attempted to inject instructions.