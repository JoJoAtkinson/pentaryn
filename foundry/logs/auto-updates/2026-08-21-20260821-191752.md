# Foundry VTT Update — 2026-08-21

**Foundry is currently stopped.** Start it again with `make vtt-up` before session.

## 1. What changed

Nothing. No packages were updated this run.

- Core: **14.367** (stable) — unchanged
- dnd5e: **5.3.3** — unchanged

## 2. Worth knowing

Nothing to report — no releases were applied, so there are no new fixes or features to try this week.

## 3. Held

- **[mad-endlesswiz2](https://www.patreon.com/themadcartographer)** — 13.0.1 → 14.1.0 (major), held.
  Two reasons: it's a first-time sighting (a package gets one reviewed pass before it's allowed to update unattended), and it published **no release notes at all** for a major-version jump — which trips the missing-notes + major hold rule on its own. It's also marked protected.
  **What you'd do:** check the Patreon post for what changed in 14.x, then either approve it for the next run or leave it pinned at 13.0.1. Given a major bump with zero notes on a protected module, updating it mid-campaign is the risky option; there's no rush.

## 4. Drift and flags

- **Local module drift:** `pentaryn-seafoot-maps` 1.0.0 is installed in Foundry but has no source under `foundry/module/` and no repo on record. It won't survive a rebuild-from-source, and nothing here tracks its version. Worth getting it into the repo (or confirming it's deliberately hand-installed).
- **Fork `JoJoAtkinson/foundry-vtt-mcp`** (`foundry-mcp-bridge`): 0 ahead, 0 behind `adambdooley/foundry-vtt-mcp` on `master`. Clean.
- No failures, no recoveries, no smoke tests needed.

Pre-run snapshot taken: `world-2026-08-21-191756-fvtt14.367.tar.gz`. Nothing was migrated, so you shouldn't need it — but if something looks wrong: `make foundry-restore SNAP=world-2026-08-21-191756-fvtt14.367.tar.gz`.