# Operating System Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

.output/
.artifacts/
.cache/

temp

sessions/**/outputs

.history/

.timeline_data/
output/
# Generated timeline SVGs should publish into `docs/` (Pages), not under `world/`.
world/**/_history.*.svg
world/**/_timeline.*.svg

# Editor Files
.idea/
*.swp
*.swo
*.bak
*~

# Temporary Files
/tmp/
*.tmp
*.temp

# Personal Notes (if you want to keep private notes)
# Uncomment the following if you want to exclude certain private directories
# /personal-notes/
# /dm-secrets/

# Build artifacts (if using any tools to generate PDFs, etc.)
*.pdf
*.aux
*.log

# Node modules (if using any Node.js tools)
node_modules/
package-lock.json

# Python / uv
.venv/
.venv-cron/
venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/

# Codex (local)
.codex/config.toml
.codex/config.local.toml

# Local secrets
.env

# Combat-runner per-encounter auto-memory (per-machine state)
combat-runner/.memory/

# Combat-runner scenario-test metrics (per-run output, machine-local)
combat-runner/tests/.metrics/

# Combat-runner testbot per-run output (run JSONs, cron logs, decisions, etc.)
# scenarios.yml + thrulm cron config are hand-authored config inputs — keep tracked.
# Ignore dir *contents* (not the dir itself) so the negation can re-include the files.
combat-runner/.testbot/*
!combat-runner/.testbot/scenarios.yml
!combat-runner/.testbot/*-cron-prompt.md
!combat-runner/.testbot/*-rotation.json
!combat-runner/.testbot/playtest-*.md
!combat-runner/.testbot/decisions/
combat-runner/.testbot/decisions/*
!combat-runner/.testbot/decisions/*.md
