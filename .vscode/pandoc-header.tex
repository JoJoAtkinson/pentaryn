% Extra layout tweaks for Pandoc-generated PDFs
\setlength{\columnsep}{1cm}% widen space between columns
\raggedright% left-align text (no full justification)

% Make pandoc's longtable behave like a normal tabular in two-column docs
\usepackage{booktabs} % usually already loaded by pandoc, but safe
\usepackage{longtable}

\makeatletter
% Replace longtable with a centered tabular
\renewenvironment{longtable}{%
  \begin{center}\small\begin{tabular}%
}{%
  \end{tabular}\end{center}%
}

% Pandoc's longtable setup uses these; make them no-ops / simple rules
\renewcommand{\endhead}{}
\renewcommand{\endfirsthead}{}
\renewcommand{\endfoot}{}
\renewcommand{\endlastfoot}{}

% If pandoc uses booktabs-style rules:
\renewcommand{\toprule}{\hline}
\renewcommand{\midrule}{\hline}
\renewcommand{\bottomrule}{\hline}
\makeatother

% Allow table columns to wrap and shrink
\renewcommand{\arraystretch}{1.2}
\setlength{\parindent}{0pt}
\setlength{\tabcolsep}{2pt}  % reduce column padding from default 6pt

% --- Unicode symbol + color-emoji rendering (LuaLaTeX only) ---
% Under LuaLaTeX, fall the main font back to Arial Unicode MS (arrows, ≤, ⚠, …)
% and then Apple Color Emoji (😀 🎲 ✅) for any glyph the main font lacks.
% Guarded by \ifLuaTeX so pdfLaTeX/XeLaTeX builds are unaffected.
\usepackage{iftex}
\ifLuaTeX
  \usepackage{fontspec}
  \directlua{luaotfload.add_fallback("emojifallback", {
    "Arial Unicode MS:mode=harf;",
    "Apple Color Emoji:mode=harf;"
  })}
  \setmainfont{Baskerville}[RawFeature={fallback=emojifallback}]
\fi
